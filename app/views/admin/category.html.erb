<h1><%=link_to 'UI認証ページ',:action=> :index%> /カテゴリー編集</h1>

<table class="table" style="width:700px;">
  <thead>
    <tr>
      <th>ID</th>
      <th>カテゴリー</th>
      <th>編集</th>
    <tr>
  </thead>
  <tbody>
<% @categories.each do |category| %>
    <tr>
      <td><%= category.id %></td>
      <td><%=category.category_name %></td>
      <td><%=link_to '編集', :action=> :edit_category, :id =>category.id %></td>
    </tr>
<% end %>
  </tbody>
<table>
